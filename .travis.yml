language: c
compiler:
  - clang
sudo: false
addons:
  apt:
    packages:
    - make
    - autoconf
    - build-essential
    - libcurl4-openssl-dev
    - libtool
    - libjansson-dev
    - libcunit1-dev
before_install:
  - cd src
script:
  - make
  - make test ; sh test
